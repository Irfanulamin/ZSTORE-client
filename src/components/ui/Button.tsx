import { Button as NextUIButton } from "@nextui-org/button";

const CustomButton = () => {
  return (
    <div>
      <NextUIButton>Click me</NextUIButton>
    </div>
  );
};

export default CustomButton;
